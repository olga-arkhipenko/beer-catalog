import ajaxHelper from 'common/helpers/ajaxHelper';
import localStorageHelper from 'common/helpers/localStorageHelper';
import urlMapper from 'features/utilities/punkApiMapping/urlMapper';

export default {
    fetchBeers() {

    }
}